@{ 
    Layout = null;
}

<script>
    $(function () {
        $("#form-Color").submit(function () {
            var data = $(this).serialize();
            var href = $(this).attr("action");
            $.request(href, null, function (json) {
                if (json.result == 1) {

                    alert(json.content);
                    location.href = "/Color/Index";
                } else {
                    alert(json.content);
                }
            });
            return false;
        });

    });
</script>


<div class="page-header">
    <h3 class="text-center">添加平板颜色</h3>
</div>

<div class="row">
    <div class="col-md-12">
        <form class="form-horizontal" method="post" action="" id="form-Color">
            <div class="form-group">
                <label class="control-label col-md-3">颜色名称：</label>
                <div class="col-md-7">
                    <input type="text" name="Name" class="form-control" placeholder="颜色名称" />
                </div>
            </div>
            <div class="form-group">
                <div class="col-md-7 col-md-offset-3">
                    <button type="submit" class="btn btn-primary btn-sm"><i class="glyphicon glyphicon-ok"></i>添加</button>
                </div>
            </div>
        </form>
    </div>
</div>

